# Airgap Demo Workspace

This is a tiny field-code example for the zipcode demo. A disaster response team
uses a local script to parse water sensor readings from USB drives. The laptop is
offline, so the coding assistant must inspect files locally.

Known issue: invalid readings sometimes pass validation.

TODO: tighten sensor value validation without changing the CSV format.
